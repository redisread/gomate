# GoMate 前端重构测试指南

## 🎯 测试目标

确保重构后的所有功能正常工作,用户体验流畅。

## 📋 测试前准备

### 1. 启动开发服务器
```bash
npm run dev
```
服务器地址: http://localhost:3000

### 2. 测试账号
- 账号 1: `wujiahong2013@gmail.com` / `11111111`
- 账号 2: `1427298682@qq.com` / `11111111`

## 🧪 测试清单

### 一、API 测试

#### 1. Locations API
```bash
# 测试命令
curl http://localhost:3000/api/locations | jq '.success'

# 预期结果
true

# 验证点
- 返回 success: true
- locations 数组包含地点数据
- 每个 location 包含 routes 数组
- 兼容层字段存在 (difficulty, duration, distance)
```

#### 2. Routes API
```bash
# 测试命令
curl http://localhost:3000/api/routes | jq '.success'

# 预期结果
true

# 验证点
- 返回 success: true
- routes 数组包含路线数据
- 每条路线包含完整信息
```

#### 3. Teams API
```bash
# 测试命令
curl http://localhost:3000/api/teams | jq '.success'

# 预期结果
true
```

### 二、页面功能测试

#### 1. 首页 / 地点列表页
**URL**: http://localhost:3000

**测试步骤**:
1. 打开首页
2. 观察地点卡片展示

**验证点**:
- [ ] 地点卡片显示正确
- [ ] 显示"X条路线"而非单一路线信息
- [ ] 显示难度范围(如"简单-困难")
- [ ] 城市标签显示正确
- [ ] 点击卡片可跳转到地点详情页

#### 2. 地点详情页(单路线)
**URL**: http://localhost:3000/locations/qiniangshan

**测试步骤**:
1. 从首页点击进入地点详情页
2. 观察页面布局和内容

**验证点**:
- [ ] 地点基本信息显示正确
- [ ] LocationInfoCard 显示地点信息(最佳季节、设施、标签)
- [ ] LocationInfoCard 显示路线列表
- [ ] 如果只有一条路线,直接显示 RouteGuide
- [ ] RouteGuide 显示完整路线信息
- [ ] 队伍列表显示正确

#### 3. 地点详情页(多路线)
**URL**: 选择一个有多条路线的地点

**测试步骤**:
1. 进入有多条路线的地点详情页
2. 观察路线列表
3. 点击选择不同路线

**验证点**:
- [ ] 显示 RouteList 组件
- [ ] 路线列表展示所有路线
- [ ] 难度筛选功能正常
- [ ] 点击路线可查看详情
- [ ] 选中路线后显示 RouteGuide
- [ ] 队伍列表根据选中路线筛选

#### 4. 路线详情页
**URL**: http://localhost:3000/routes/[route_id]

**测试步骤**:
1. 从地点详情页点击路线卡片
2. 或从 LocationInfoCard 的路线列表点击

**验证点**:
- [ ] 页面标题显示"地点名 - 路线名"
- [ ] 返回按钮可回到地点详情页
- [ ] RouteInfoCard 显示路线关键信息
- [ ] RouteGuide 显示完整路线指南
- [ ] 显示该路线的队伍列表
- [ ] "创建队伍"按钮可用
- [ ] 点击创建队伍跳转到创建页面,并预选该路线

#### 5. 创建队伍页面
**URL**: http://localhost:3000/teams/create

**测试步骤 A - 从地点详情页进入**:
1. 在地点详情页点击"创建队伍"
2. 观察表单内容

**验证点**:
- [ ] 地点已预选
- [ ] 显示该地点的所有路线
- [ ] 路线选择 UI 清晰(单选按钮卡片)
- [ ] 每条路线显示详细信息(难度、时长、距离)
- [ ] 如果只有一条路线,自动选中

**测试步骤 B - 从路线详情页进入**:
1. 在路线详情页点击"创建队伍"
2. 观察表单内容

**验证点**:
- [ ] 地点已预选
- [ ] 路线已预选(从 URL 参数)
- [ ] 路线选择区域正确高亮选中的路线

**测试步骤 C - 提交表单**:
1. 填写完整表单信息
2. 选择一条路线
3. 点击"创建队伍"

**验证点**:
- [ ] 必须选择路线才能提交
- [ ] 提交成功后跳转到队伍详情页
- [ ] 队伍详情页显示正确的路线信息

#### 6. 队伍详情页
**URL**: http://localhost:3000/teams/[team_id]

**验证点**:
- [ ] 显示关联的路线信息
- [ ] 路线名称和难度正确
- [ ] 可以查看路线详情

### 三、数据流测试

#### 1. LocationsContext
**测试方法**: 在浏览器控制台

```javascript
// 检查 locations 数据
console.log(window.__LOCATIONS_DATA__)

// 验证点
- 每个 location 包含 routes 数组
- 兼容层字段存在
```

#### 2. TeamsContext
**测试方法**: 创建队伍后检查

```javascript
// 验证 team 数据包含 routeId
console.log(team.routeId)
```

### 四、组件测试

#### 1. RouteCard
**位置**: 路线列表页、地点详情页

**验证点**:
- [ ] default 变体显示完整信息
- [ ] compact 变体显示简化信息
- [ ] 难度徽章颜色正确
- [ ] 点击可跳转或触发回调

#### 2. RouteList
**位置**: 地点详情页(多路线场景)

**验证点**:
- [ ] 显示路线数量
- [ ] 难度筛选功能正常
- [ ] 空状态提示友好
- [ ] 响应式布局正常

#### 3. RouteInfoCard
**位置**: 路线详情页

**验证点**:
- [ ] 显示所有路线关键信息
- [ ] 建议装备列表显示
- [ ] 警告提示突出显示
- [ ] 标签展示正确

#### 4. LocationCard (更新后)
**位置**: 首页、地点列表

**验证点**:
- [ ] 显示路线数量而非单一路线信息
- [ ] 难度范围计算正确
- [ ] 所有变体(default, compact, horizontal)正常

#### 5. LocationInfoCard (更新后)
**位置**: 地点详情页

**验证点**:
- [ ] 只显示地点信息,不显示路线信息
- [ ] 路线列表可点击跳转
- [ ] 设施图标显示正确

### 五、边界情况测试

#### 1. 无路线的地点
**测试场景**: 地点没有关联路线

**验证点**:
- [ ] 地点详情页正常显示
- [ ] 显示友好的提示信息
- [ ] 不显示 RouteGuide
- [ ] 创建队伍时提示无可用路线

#### 2. 单路线的地点
**测试场景**: 地点只有一条路线

**验证点**:
- [ ] 不显示 RouteList
- [ ] 直接显示 RouteGuide
- [ ] 创建队伍时自动选中该路线

#### 3. 多路线的地点
**测试场景**: 地点有多条路线

**验证点**:
- [ ] 显示 RouteList
- [ ] 可选择不同路线查看详情
- [ ] 队伍列表根据选中路线筛选

### 六、性能测试

#### 1. 页面加载速度
**测试工具**: Chrome DevTools (Network tab)

**验证点**:
- [ ] 首屏加载时间 < 2秒
- [ ] API 响应时间 < 500ms
- [ ] 无不必要的重复请求

#### 2. 渲染性能
**测试工具**: React DevTools Profiler

**验证点**:
- [ ] 无不必要的组件重新渲染
- [ ] 列表滚动流畅
- [ ] 交互响应及时

### 七、响应式测试

**测试设备**:
- [ ] 桌面端 (1920x1080)
- [ ] 平板端 (768x1024)
- [ ] 手机端 (375x667)

**验证点**:
- [ ] 布局适配正确
- [ ] 文字大小合适
- [ ] 按钮可点击
- [ ] 图片不变形

### 八、类型检查

```bash
# 运行类型检查
npm run type-check

# 预期结果
无类型错误
```

### 九、构建测试

```bash
# 运行构建
npm run build

# 预期结果
构建成功,无错误和警告
```

## 🐛 已知问题

记录测试中发现的问题:

### 问题 1
- **描述**:
- **重现步骤**:
- **预期行为**:
- **实际行为**:
- **优先级**: 高/中/低

## ✅ 测试签收

测试完成后,请在此签名确认:

- [ ] API 测试通过
- [ ] 页面功能测试通过
- [ ] 组件测试通过
- [ ] 边界情况测试通过
- [ ] 性能测试通过
- [ ] 响应式测试通过
- [ ] 类型检查通过
- [ ] 构建测试通过

**测试人**: ___________
**测试日期**: ___________
**测试环境**: ___________

## 📝 备注

其他需要记录的信息:
