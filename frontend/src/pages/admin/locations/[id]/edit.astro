---
/**
 * 管理员地点编辑页 - 动态路由
 */
import Layout from "../../../../layouts/Layout.astro";
import { LocationEditClient } from "../../../../components/features/location-edit-client";

export function getStaticPaths() {
  return [{ params: { id: "fallback" } }];
}

const { id } = Astro.params;
---

<Layout title="编辑地点 - GoMate" showNavbar={false} showFooter={false}>
  <LocationEditClient locationId={id!} client:load />
</Layout>
