---
/**
 * 队伍详情页 - 动态路由
 */
import Layout from "../../layouts/Layout.astro";
import { TeamDetailClient } from "../../components/features/team-detail-client";

const { id } = Astro.params;
---

<Layout title="队伍详情 - GoMate" showNavbar={false} showFooter={false}>
  <TeamDetailClient teamId={id!} client:load />
</Layout>
